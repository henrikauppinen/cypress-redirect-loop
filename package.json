{
  "name": "cypress-redirect-loop",
  "dependencies": {
    "cypress": "^5.6.0",
    "http-server": "^0.12.3"
  },
  "scripts": {
    "start": "npx http-server pub",
    "test": "npx cypress open --config baseUrl=http://127.0.0.1:8080/"
  }
}
